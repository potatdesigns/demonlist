# Generated by scripts/refresh-*.mjs at runtime (locally or in CI) and
# published to the `cache` branch (see README's "Cache branch" section)
# instead of being committed here — main no longer tracks these.
/data/*.json

# wrangler's local build cache (worker/) — machine-local, not deploy state.
.wrangler/
